/* eslint-disable no-unused-vars */
function includes(array, value) {
  for (let i = 0; i < array.length; i++) {
    if (value === array[i]) return true;
  }
  return false;
}

// create a for loop X
// within the loop, check if value equals current index of the array X
// return true if true X
// return false outside the for loop X
