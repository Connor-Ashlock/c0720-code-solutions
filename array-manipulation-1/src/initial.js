/* eslint-disable no-unused-vars */
function initial(array) {
  const newArr = [];
  for (let i = 0; i < array.length - 1; i++) {
    newArr.push(array[i]);
  }
  return newArr;
}

// define a variable initialized with an empty array X
// create a for loop that loops through all of the indexs minus the last one X
// push all of the looped thorugh indexs into the new array X
// return the new array X
