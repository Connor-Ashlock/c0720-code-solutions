/* eslint-disable no-unused-vars */
function toObject(keyValuePair) {
  const obj = {};
  obj[keyValuePair[0]] = keyValuePair[1];
  return obj;
}

// declare a variable initialized with and empty object literal X
// assign the value of index 1 to the property (which is index 0) of the empty object literal X
// return the object X
